'use client';

/**
 * Agents hub surface — library + onboarding/chat are composed in {@link AutomationsHubView}.
 * Kept as a named module for a consistent hub workspace layout API.
 */
export { default } from '@/components/agents/AgentManagementView';
