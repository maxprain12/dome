/**
 * Settings-facing aliases for the shared Codex hub blocks.
 * Prefer importing Hub* from `@/components/hub` in new hubs;
 * Settings sections may keep these names.
 */

export {
  HubSurface as SettingsSurface,
  HubGroup as SettingsGroup,
  HubRow as SettingsRow,
} from '@/components/hub';
