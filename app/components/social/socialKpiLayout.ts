/** Shared fixed footprint for Social hub header KPIs (all tabs). */
export const SOCIAL_KPI_STRIP_CLASS = 'flex gap-2 overflow-x-auto pb-0.5';

/** Width + height locked so growth cards and metric pills align across tabs. */
export const SOCIAL_KPI_CARD_CLASS =
  'box-border flex h-[5.75rem] w-[10.5rem] shrink-0 flex-col gap-0.5 overflow-hidden px-2.5 py-2 text-left';
