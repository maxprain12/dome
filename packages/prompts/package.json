{
  "name": "@dome/prompts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Prompt sections + assembler. Owns every piece of prompt text and the system-prompt builder.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"@dome/prompts: no tests yet\" && exit 0"
  }
}
