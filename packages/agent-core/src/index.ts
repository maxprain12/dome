// @dome/agent-core public API
// Dome-native agent runtime — replaces LangGraph + deepagents + middleware chain
// + SqliteSaver with a single `runAgentLoop`, typed hooks, session repo, compaction,
// and skills injection. Knows nothing about Electron, IPC, or React.
// This package is Node-only — the renderer must not import it (R9).
export {};
