{
  "name": "@dome/agent-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dome-native agent runtime (LangGraph replacement). runAgentLoop, hooks, session, compaction, skills.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"@dome/agent-core: no tests yet\" && exit 0"
  }
}
