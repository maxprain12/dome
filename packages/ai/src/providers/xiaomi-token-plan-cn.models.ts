import type { Api, Model } from "../types.js";
import { MODELS } from "../models.legacy-catalog.js";

export const XIAOMI_TOKEN_PLAN_CN_MODELS = MODELS["xiaomi-token-plan-cn"] as unknown as Record<string, Model<Api>>;
