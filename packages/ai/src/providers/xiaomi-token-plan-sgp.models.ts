import type { Api, Model } from "../types.js";
import { MODELS } from "../models.legacy-catalog.js";

export const XIAOMI_TOKEN_PLAN_SGP_MODELS = MODELS["xiaomi-token-plan-sgp"] as unknown as Record<string, Model<Api>>;
