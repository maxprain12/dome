/** Legacy schema_version at HEAD before Drizzle-managed migrations. */
export const LEGACY_SCHEMA_VERSION = 53;

/** Drizzle journal tag for the baseline that mirrors legacy v53. */
export const DRIZZLE_BASELINE_TAG = '0000_baseline_v53';
