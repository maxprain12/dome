// @dome/tools public API
// Agent tool registry: one module per tool family. Definitions (schema) are
// renderer-safe; execution is main-process only.
// This package is Node-only — the renderer must not import it (R9).
export {};
