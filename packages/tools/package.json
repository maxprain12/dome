{
  "name": "@dome/tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent tool registry: one module per tool family. Definitions (schema) are renderer-safe; execution is main-process only.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"@dome/tools: no tests yet\" && exit 0"
  }
}
